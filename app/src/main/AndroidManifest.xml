<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.buildacake">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FrequentlyAskedQuestions"
            android:label="@string/frequently_asked_questions"/>
        <activity
            android:name=".AboutUs"
            android:label="@string/activity_about_us" />
        <activity
            android:name=".CakesOrdered"
            android:label="@string/activity_order_details" />
        <activity
            android:name=".OrderDetails"
            android:label="@string/activity_order_summary"/>
        <activity
            android:name=".BuildACake"
            android:label="@string/activity_build_cake_name" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>